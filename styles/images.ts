import { Image } from "react-native";
import unknowArtist from "../assets/images/unknown_artist.png";
import unknowTrack from "../assets/images/unknown_track.png";
export const unknowArtistImageUri = Image.resolveAssetSource(unknowArtist).uri;
export const unknowTrackImageUri = Image.resolveAssetSource(unknowTrack).uri;
